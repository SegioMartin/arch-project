spring:
  datasource:
    url: jdbc:postgresql://account-postgres:5432/account_db
    username: account_user
    password: account_pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  test:
    database:
      replace: none
